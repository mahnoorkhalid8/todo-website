apiVersion: v1
kind: Service
metadata:
  name: backend
  namespace: todo-chatbot
  labels:
    app: backend
spec:
  type: ClusterIP
  ports:
    - port: 8000
      targetPort: 8000
      protocol: TCP
      name: http
  selector:
    app: backend
  sessionAffinity: None
